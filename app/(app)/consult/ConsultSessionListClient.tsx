'use client';

// Client component for ConsultSessionList with data fetching
import ConsultSessionList from '@/components/consult/ConsultSessionList';

export default function ConsultSessionListClient() {
  return <ConsultSessionList />;
}
